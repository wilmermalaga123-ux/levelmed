from django.apps import AppConfig


class FlashcardsPremiumConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.flashcards_premium'
    verbose_name = 'Flashcards Premium'
